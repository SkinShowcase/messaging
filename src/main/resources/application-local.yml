# Локальный профиль: секрет для проверки JWT (тот же, что в auth при локальной разработке)
app:
  auth:
    jwt:
      secret: ${AUTH_JWT_SECRET:local-dev-secret-must-be-at-least-32-bytes-long-for-hs256}

messaging:
  tcp:
    port: 9090
